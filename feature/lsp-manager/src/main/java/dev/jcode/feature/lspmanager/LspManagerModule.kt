package dev.jcode.feature.lspmanager

/**
 * LSP Manager UI: install / verify / remove language servers inside the active distro.
 * The catalog is sourced from `LspServerDescriptor.BUILT_IN`; the action machinery lives in
 * `DistroService`.
 */
object LspManagerModule
