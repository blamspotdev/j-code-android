<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <!-- On-demand: the engine is delivered by the Web Engine marketplace extension, never
         fused into the base APK. GeckoView's own manifest (child-process services, etc.)
         merges in from its AAR. -->
    <dist:module
        dist:instant="false"
        dist:title="Web Engine">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="false" />
    </dist:module>

</manifest>
