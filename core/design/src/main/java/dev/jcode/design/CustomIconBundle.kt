package dev.jcode.design

/**
 * Custom vector icon packs. Each is an [IconBundle] whose overrides are hand-authored [ImageVector]s
 * (see the builders below) and which falls back to [defaultIconBundle] for any slot it doesn't
 * restyle. Populated in a later step.
 */
val customIconBundles: List<IconBundle> = emptyList()
